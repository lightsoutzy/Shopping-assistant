[build]
builder = "dockerfile"

[deploy]
# startCommand intentionally omitted — the Dockerfile CMD handles port binding.
# CMD uses shell form so ${PORT:-8000} expands correctly from Railway's $PORT env var.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
