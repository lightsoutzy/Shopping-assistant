# Backend-only requirements — used by Railway (via nixpacks.toml).
# torch is pinned to the CPU-only build to keep the Railway image under 4 GB.
# The CUDA build from PyPI (~3.5 GB installed) exceeds Railway's 4 GB image limit;
# the CPU build (~800 MB installed) does not. CLIP inference is CPU-bound at demo scale.
# For local development, use requirements.txt instead.

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.111.0
uvicorn[standard]==0.29.0
anthropic>=0.40.0
pandas>=2.3.3,<2.4
numpy>=2.3.4,<2.4
scikit-learn>=1.7,<1.9
Pillow>=12,<13
python-dotenv==1.0.1
pyarrow>=22,<24
torch>=2.9,<2.12
transformers>=4.57,<5
